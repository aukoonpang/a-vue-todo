const todo = {
  todoList: state => state.todo.todoList,
  filterState: state => state.todo.filterState,
  toastShow: state => state.todo.toastShow,
  toastConfig: state => state.todo.toastConfig
}

export default todo
